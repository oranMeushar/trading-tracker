import React from 'react';
import { Container } from './Reports.style';
import withNavigation from '../../hocs/withNavigation/withNavigation';

const Reports = () => {
    return (
        <Container>
            Reports
        </Container>
    );
};

export default withNavigation(Reports);